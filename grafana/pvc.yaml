apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pv-claimg
  namespace: monitoring
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
